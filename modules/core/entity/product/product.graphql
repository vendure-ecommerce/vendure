type Product {
    id: Int
    name: String
    slug: String
    description: String
    image: String
    variants: [ProductVariant]
    optionGroups: [ProductOptionGroup]
    createdAt: String
    updatedAt: String
}
