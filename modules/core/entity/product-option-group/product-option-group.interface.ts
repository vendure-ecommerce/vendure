import { LocaleString } from '../../locale/locale-types';

export interface ProductOptionGroup {
    id: number;
    code: string;
    name: LocaleString;
    createdAt: string;
    updatedAt: string;
}
