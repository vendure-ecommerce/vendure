{
  "name": "@vendure/payments-plugin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/payments-plugin/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages/payments-plugin"
      },
      "outputs": ["{projectRoot}/lib"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --fix .",
        "cwd": "packages/payments-plugin"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest --run",
        "cwd": "packages/payments-plugin"
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest --run",
        "cwd": "packages/payments-plugin/e2e"
      },
      "dependsOn": ["build"]
    },
    "ci": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages/payments-plugin"
      },
      "outputs": ["{projectRoot}/package"],
      "dependsOn": ["^ci"]
    }
  },
  "tags": ["type:plugin", "scope:payments"]
}
