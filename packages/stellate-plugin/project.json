{
  "name": "@vendure/stellate-plugin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/stellate-plugin/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages/stellate-plugin"
      },
      "outputs": ["{projectRoot}/lib"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --fix .",
        "cwd": "packages/stellate-plugin"
      }
    }
  },
  "tags": ["type:plugin", "scope:caching"]
}
