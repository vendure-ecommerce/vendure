{
  "name": "@vendure/common",
  "version": "0.1.2-beta.4",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json -w",
    "build": "rimraf dist && tsc -p ./tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "lib/**/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^2.6.3",
    "@types/jest": "^24.0.11",
    "typescript": "^3.3.4000"
  }
}
