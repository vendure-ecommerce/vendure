type GlobalSettings {
    id: ID!
    createdAt: DateTime!
    updatedAt: DateTime!
    availableLanguages: [LanguageCode!]!
    trackInventory: Boolean!
    serverConfig: ServerConfig!
}

type ServerConfig {
    customFields: JSON
}
