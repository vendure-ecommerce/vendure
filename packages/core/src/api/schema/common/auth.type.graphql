type CurrentUser {
    id: ID!
    identifier: String!
    channels: [CurrentUserChannel!]!
    sessionToken: String!
}

type CurrentUserChannel {
    id: ID!
    token: String!
    code: String!
    permissions: [Permission!]!
}
