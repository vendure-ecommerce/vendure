interface PaginatedList {
    items: [Node!]!
    totalItems: Int!
}

interface Node {
    id: ID!
}

input ListOptions {
    take: Int
    skip: Int
    sort: [SortParameter!]
}

enum SortOrder {
    ASC
    DESC
}

input SortParameter {
    field: String!
    order: SortOrder
}

scalar JSON
