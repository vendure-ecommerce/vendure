type GlobalSettings {
    id: ID!
    createdAt: DateTime!
    updatedAt: DateTime!
    availableLanguages: [LanguageCode!]!
    serverConfig: ServerConfig!
}

type ServerConfig {
    customFields: JSON
}

input UpdateGlobalSettingsInput {
    availableLanguages: [LanguageCode!]
}
