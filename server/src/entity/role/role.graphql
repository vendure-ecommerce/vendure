type Role implements Node {
    id: ID!
    code: String!
    description: String!
    permissions: [String!]!
    channels: [Channel!]!
}

input CreateRoleInput {
    code: String!
    description: String!
    permissions: [String!]!
}
