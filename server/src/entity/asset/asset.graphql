type Asset implements Node {
    id: ID!
    name: String!
    description: String
    type: AssetType!
    fileSize: Int!
    mimeType: String!
    source: String!
    preview: String!
}

input CreateAssetInput {
    file: Upload!
}
