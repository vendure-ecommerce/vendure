type Order implements Node {
    id: ID!
    createdAt: DateTime!
    updatedAt: DateTime!
    code: String!
    state: String!
    customer: Customer
    lines: [OrderLine!]!
    adjustments: [Adjustment!]!
    subTotalBeforeTax: Int!
    subTotal: Int!
    totalBeforeTax: Int!
    total: Int!
}
