type Order implements Node {
    id: ID!
    createdAt: DateTime!
    updatedAt: DateTime!
    code: String!
    customer: Customer
    lines: [OrderLine!]!
    totalPriceBeforeTax: Int!
    totalPrice: Int!
}
