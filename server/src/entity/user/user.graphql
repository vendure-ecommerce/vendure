type User implements Node {
    id: ID!
    createdAt: DateTime!
    updatedAt: DateTime!
    identifier: String!
    passwordHash: String!
    verified: Boolean!
    roles: [Role!]!
    lastLogin: String
}
