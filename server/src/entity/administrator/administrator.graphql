type Administrator implements Node {
    id: String!
    firstName: String
    lastName: String
    emailAddress: String
    user: User
}

input CreateAdministratorInput {
    firstName: String
    lastName: String
    emailAddress: String!
    password: String!
}
