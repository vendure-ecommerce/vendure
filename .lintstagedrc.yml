linters:
  admin-ui/**/*.ts:
    - yarn lint:admin-ui
    - yarn format
    - git add
  admin-ui/**/*.html:
    - yarn format
    - git add
  packages/core/*.ts:
    - yarn lint:core
    - yarn format
    - git add
